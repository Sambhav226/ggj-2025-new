Is a basic game about simplified economy

Uses python

To run make virtualenv, install requirements.txt and then

```
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt 
python main.py
```